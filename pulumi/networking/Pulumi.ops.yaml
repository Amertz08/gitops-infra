config:
  aws:region: us-east-1
  networking:availabilityZones:
    - us-east-1a
    - us-east-1b
  networking:vpcCidr: 10.0.0.0/16
  networking:publicSubnetCidrs:
    - 10.0.128.0/24
    - 10.0.129.0/24
  networking:privateSubnetCidrs:
    - 10.0.0.0/20
    - 10.0.16.0/20
  networking:eksInstanceType: m5.large
  networking:eksNodeCount: 3
  networking:vpnClientCidr: 172.16.0.0/22
  # Set before deploying:
  #   pulumi config set --secret networking:vpnServerCertArn arn:aws:acm:...
  #   pulumi config set --secret networking:vpnClientCaArn  arn:aws:acm:...
